#include <bits/stdc++.h>
using namespace std;
int main()
{
    int exponent = 1;
    int n;
    int x;
    cin >> n >> x;
    int i = 1;
    while (i < x)
    {
        int d = n * n;
        i++;
    }

    cout << exponent << endl;
}
