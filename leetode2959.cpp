#include <bits/stdc++.h>
using namespace std;

class Solution
{
public:
    vector<int>
    evenOddBit(int n)
    {
        string s = " ";
        if (n != 0)
        {
            int x = n % 2;
            s = to_string(x) + s;
        }
    }
};