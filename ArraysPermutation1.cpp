#include <bits/stdc++.h>
using namespace std;
void permutation(char a[], char b[])
{
    sort(a, a + a1);
    sort(b, b + b1);
}
int main()
{
    int i, j;
    char a[100];
    cin >> a;
    char b[100];
    cin >> b;
    permutation(a, b);
}